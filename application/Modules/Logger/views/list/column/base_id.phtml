<?php
namespace JetApplicationModule\Logger;

use Jet\MVC;
use Jet\MVC_View;

/**
 * @var MVC_View $this
 * @var Event    $item
 * @var Listing  $listing
 */
$item = $this->getRaw( 'item' );
$listing = $this->getRaw( 'listing' );

echo MVC::getBase($item->getBaseId())?->getName();

