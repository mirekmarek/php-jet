<?php

use Jet\MVC;
use Jet\Tr;

?>
<div class="card bg-light text-dark">
	<div class="card-body">

		<table class="table">
			<tr>
				<td><?= Tr::_( 'Current base id:' ) ?></td>
				<td><?= MVC::getBase()->getId(); ?></td>
			</tr>
			<tr>
				<td><?= Tr::_( 'Current locale:' ) ?></td>
				<td><?= MVC::getLocale(); ?></td>
			</tr>
			<tr>
				<td><?= Tr::_( 'Current page id:' ) ?></td>
				<td><?= MVC::getPage()->getId(); ?></td>
			</tr>
		</table>

	</div>
</div>