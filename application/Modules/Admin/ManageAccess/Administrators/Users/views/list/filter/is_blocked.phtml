<?php
namespace JetApplicationModule\Admin\ManageAccess\Administrators\Users;

use Jet\MVC_View;

/**
 * @var MVC_View $this
 * @var Listing $listing
 */
$listing = $this->getRaw( 'listing' );
$filter_form = $listing->getFilterForm();
?>
<div style="display: grid;align-items: center;gap: 10px;grid-template-columns: 120px 120px;">
	<?= $filter_form->field( 'is_blocked' )->label() ?>
	<div><?= $filter_form->field( 'is_blocked' )->input()->addJsAction( 'onchange', 'this.form.submit()' ) ?></div>
</div>

