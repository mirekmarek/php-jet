<?php

use Jet\MVC_View;
use Jet\Form_Renderer;

/**
 * @var MVC_View $this
 * @var Form_Renderer $r
 */
$r = $this->getRaw( 'element' );
$form = $r->getForm();

if( !$form->getCommonMessage() ) {
	return;
}
?>
<div <?=$r->renderAttributes([])?>><?= $form->getCommonMessage() ?></div>