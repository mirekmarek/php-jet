<?php

use Jet\UI_tabsJS;
use Jet\MVC_View;

/**
 * @var MVC_View $this
 * @var UI_tabsJS $t
 */
$t = $this->getRaw( 'element' );

$t->setBaseCssClass('nav nav-tabs');
?>
<div class="tab-content">
	<ul <?=$t->renderTagAttributes()?>>
		<?php foreach( $t->getTabs() as $tab ):
			echo $tab;
		endforeach; ?>
	</ul>
