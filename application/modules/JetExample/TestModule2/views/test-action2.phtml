<?php
use Jet\Form;
use Jet\Mvc;

/**
 * @var Form $form
 */
$form = $this->form;

?>
M2: Test Action 2

<p>
	<?php if($this->form_sent): ?>
	Form SENT:
	<table>
		<?php foreach($this->form_values as $k=>$v): ?>
		<tr>
			<td><?=$k;?>:</td>
			<td><?=is_array($v) ? implode(',',$v) : $v;?></td>
		</tr>
		<?php endforeach; ?>
	</table>
	<?php endif; ?>

	<?=$form->helper_getBasicHTML();?>
</p>

