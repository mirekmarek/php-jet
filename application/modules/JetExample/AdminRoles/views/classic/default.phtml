<?php
namespace JetApplicationModule\JetExample\AdminRoles;
use Jet;

/**
 * @var Jet\Auth_Role_Abstract[] $roles
 */
$roles = $this->roles;

/**
 * @var Jet\Data_Paginator $paginator
 */
$paginator = $this->paginator;

/**
 * @var \JetApplicationModule\JetExample\AdminUIManager\Main $UI_manager
 */
$UI_manager = Jet\Mvc::getCurrentUIManagerModuleInstance();
?>

<div style="padding: 10px;">

<a href="?new" class="btn btn-primary"><?=Jet\Tr::_('Add new role');?></a>

<table class="table table-striped">
	<thead>
		<tr>
			<th style="width: 40px;"></th>
			<th><?=Jet\Tr::_('ID');?></th>
			<th><?=Jet\Tr::_('Name');?></th>
			<th><?=Jet\Tr::_('Description');?></th>
		</tr>
	</thead>
	<tbody>
<?php foreach( $roles as $role ): ?>
	<tr>
		<td>
			<a href="?ID=<?php echo rawurlencode( $role->getID() ); ?>" class="btn btn-info">EDIT</a>
		</td>
		<td><?=$role->getID(); ?></td>
		<td><?=$role->getName(); ?></td>
		<td><?=$role->getDescription(); ?></td>
	</tr>
<?php endforeach; ?>
	</tbody>
	<tfoot>
	<tr>
		<td colspan="4">
			<?=$UI_manager->renderPaginator( $paginator );?>
		</td>
	</tr>
	</tfoot>
</table>

</div>