<?php

use Jet\UI_button;
use Jet\UI;
use Jet\MVC_View;

/**
 * @var MVC_View $this
 * @var UI_button $b
 */
$b = $this->getRaw( 'element' );

if( !$b->getClass() ) {
	$b->setClass( UI_button::CLASS_LIGHT );
}

require 'button/_common.phtml';