<?php

use Jet\Mvc_View;
use Jet\Form_Renderer;

/**
 * @var Mvc_View $this
 * @var Form_Renderer $r
 */
$r = $this->getRaw( 'element' );

if( !$r->getBaseCssClasses() ) {
	$r->setBaseCssClass( 'form-group row' );
}

//if($r->getField()->getLastErrorMessage()) {
//	$r->addCustomCssClass('has-error has-feedback');
//}
?>
<div <?=$r->renderAttributes([])?>>
