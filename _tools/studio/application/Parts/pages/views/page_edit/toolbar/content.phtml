<?php
namespace JetStudio;

use Jet\UI;
use Jet\Tr;

$page = Pages::getCurrentPage();
$form = $page->getEditForm_content();

?>
	<?=UI::button_save()->setOnclick("$('#{$form->getId()}').submit()")?>
	<?=UI::button_create(Tr::_('Add content'))->setOnclick('JetStudio.Pages.editPage.content.addContent()')->setClass('primary')?>


