<?php
namespace JetStudio;

use Jet\Form;
use Jet\Tr;
use Jet\UI;

$page = Pages::getCurrentPage();

if(!$page) {
	return;
}
$form = $page->getEditForm();

?>

<div class="toolbar">
	<?=UI::button_save()->setOnclick("$('#{$form->getId()}').submit()")?>
	<?php /*<?=UI::button_create(Tr::_('Add content'))->setOnclick('JetStudio.Pages.editPage.content.addContent()')?>*/ ?>
</div>


