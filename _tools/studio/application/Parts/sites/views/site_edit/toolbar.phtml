<?php
namespace JetStudio;

use Jet\Tr;
use Jet\UI;

$site = Sites::getCurrentSite();

if(!$site) {
	return;
}

$form = $site->getEditForm();
?>
<div class="toolbar" id="main-toolbar">
	<?=UI::button_save()->setOnclick("$('#{$form->getId()}').submit()")?>

	<?=UI::button_create(Tr::_('Add locale'))->setOnclick('Sites.editSite.addLocale()')?>
	<?=UI::button_edit(Tr::_('Sort locales'))->setOnclick('Sites.editSite.sortLocales()')?>
</div>

