<?php
namespace JetStudio;

use Jet\UI;
use Jet\Tr;

$dialog = UI::dialog('dialog_create_new_menu_set', Tr::_('Create a new menu set'), 600);

echo $dialog->start();
	?>
	<div id="set_create_form_area">
		<?php require 'form.phtml'; ?>
	</div>
	<?php
echo $dialog->footer();
	echo UI::button_save(Tr::_('Create'))->setOnclick('JetStudio.Menus.createSet.createSend()');
echo $dialog->end();

